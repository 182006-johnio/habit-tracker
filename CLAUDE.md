# 習慣化トラッカー — 仕様

## このプロジェクトについて

個人利用専用の習慣化トラッカー。作者1人が使う。公開・配布・収益化は想定しない。

## 開発環境

- 開発は Windows PC。実行は iPhone（Safari / ホーム画面に追加した PWA）。
- macOS 前提の手順を提案しない。Xcode、iOS シミュレータ、Windows 版 Safari はいずれも使えない。
- Windows のブラウザは Blink 系のため、そこで動いても WebKit で動く保証はない。iOS 固有の挙動（共有シート経由のエクスポート、ホーム画面追加、Service Worker）は実機確認を前提に書く。
- 実機確認には HTTPS が必要。LAN の IP を平文 HTTP で叩く方法は secure context にならず、Service Worker を登録できない。静的ホスティング（GitHub Pages 等）に置いて iPhone から開く。
- データ層が動いた時点でホスティングを用意し、push すれば iPhone で確認できる状態にしておく。

## 絶対制約（変更しないこと）

- バックエンドを作らない。サーバー通信なし。
- 認証・アカウント機能を作らない。
- データはブラウザのローカル保存のみ。
- フレームワークを導入しない。素の HTML / CSS / JavaScript で書く。
- 外部ライブラリを追加しない。必要だと判断した場合は実装せず、先に提案して承認を得る。
- 外部スクリプトを読み込まない。npm パッケージも、CDN の `<script src="...">` も含む。
- ユーザー入力テキストは `textContent` で描画する。`innerHTML` を使わない。
- 最終形は PWA（ホーム画面に追加して使う）。

## データモデル

テーブルは2つだけ。派生値は保存しない。

### habits（習慣）

| フィールド | 型 | 説明 |
|---|---|---|
| id | string | 一意ID |
| name | string | 習慣名（例: 腕立て伏せ、プログラミング、読書） |
| started_on | date | 開始日 YYYY-MM-DD |
| archived | boolean | 休止中フラグ |
| order | number | 表示順 |

習慣は自由に追加できる。数の上限を設けない。

### logs（ログ）

| フィールド | 型 | 説明 |
|---|---|---|
| id | string | 一意ID |
| habit_id | string | habits.id への参照 |
| date | date | 対象日 YYYY-MM-DD |
| recorded_at | datetime | 記録した時刻 |
| rating | 0 / 1 / 2 | 2 = ○、1 = △、0 = × |
| action | text | 今日の一行動 |
| blocker | text | その日邪魔したもの |
| fix | text | 明日への修正 |

制約: `(habit_id, date)` は一意。1つの習慣につき1日1件。

連続日数・挫折回数・週まとめは、すべてこの2テーブルからの導出。カウンタを別に持たない。

## 判定ロジック

基準時刻は 0時。日付は端末のローカルタイム。

定義:

- **有効日** — rating が 2（○）または 1（△）のログがある日
- **断絶日** — 有効日でない日。rating 0（×）のログがある日と、ログが無い日の両方を含む。`started_on` より前の日は判定対象外。

### 連続日数（ストリーク）

今日から過去に遡り、有効日が連続している数を数える。

今日がまだ有効日でない場合は、昨日を起点にして数える。今日の記録がまだ無いだけでストリークを 0 表示しない。昨日も有効日でなければ 0。

### 挫折回数

`started_on` 以降で、**直前の日が断絶日である有効日**の数。

最初の有効日は復帰ではないのでカウントしない。つまり「一度切れてから戻ってきた回数」。

### 0時の締め

締め処理として状態を書き換える必要はない。上記はすべて導出計算なので、アプリを開いた時点で再計算すれば、何日ぶりに開いても正しい値になる。ログを持たない日は自動的に断絶日として扱われる。

## 週まとめ

習慣ごとに `started_on` から**暦日**で7日ずつ区切る。Week 1 = 1〜7日目、Week 2 = 8〜14日目。記録した日数ではなく暦日で数える。未記入の日も振り返りの対象として残すため。

一覧では週を畳んで表示し、週を押すとその7日分のログを開く。

表示ルール:

- 週を開いたら7日分の枠をすべて出す。ログが無い日は「未記入」の枠として表示し、飛ばさない。
- 1件もログが無い週も、空の週として表示する。週番号を詰めない。
- 未記入と ×（rating 0）は表示上は区別する。判定ロジック上はどちらも断絶日として同じ扱い。

## バックアップ（エクスポート）

ローカル保存のみの構成なので、ブラウザのデータが消えると全記録が失われる。復旧手段として書き出し機能を持つ。

- 全データを単一の JSON として書き出す。ファイル名に日付を入れる（例: `habits-2026-08-08.json`）。
- 保存先はユーザーが OS 側で選ぶ。アプリはクラウドサービスを一切知らない。API 連携も認証も追加しない。

### iOS での実装方法

ホーム画面から起動した PWA では `<a download>` によるダウンロードが機能しない。ファイルのプレビューが開いたまま、アプリに戻れなくなる。

したがって次の分岐で実装する。

1. `navigator.canShare({ files: [...] })` が true なら、`navigator.share` に File を渡して共有シートを開く。ユーザーは「"ファイル"に保存」から iCloud Drive を選ぶ。
2. 使えない場合のみ `<a download>` にフォールバックする（PC・Android 向け）。

共有シート経由の動作確認は PWA 化のあとになる。実装だけ先に済ませ、検証は後で行う。

### インポート

現時点では実装しない。必要になった時点で追加する。作る場合は**全置換のみ**とし、マージ機能は作らない。読み込んだ JSON は必ず検証してから反映する。

## UI

最後に着手する。データ層と判定ロジックが固まるまで UI を書かない。

## 実装順序

1. **データ層** — スキーマ定義、保存・読み出し、日付ユーティリティ。保存処理は1つのモジュールに閉じる（後で差し替えられるように）
2. **判定ロジック** — 連続日数と挫折回数。副作用のない純関数として書く
3. **エクスポート** — 記録が貯まる前に保険を先に置く
4. **週まとめ**
5. **UI**
6. **PWA化** — manifest、Service Worker、ホーム画面追加

1機能ずつ実装する。まとめて全部書かない。

## Claude Code への依頼ルール

- 実装の前に、どういう構造にするかを説明する。承認を得てからコードを書く。
- 差分は読める量に収める。1コミット1機能。
- この仕様に無い機能を勝手に足さない。必要だと思ったら提案する。

## テスト観点（判定ロジック）

バグが出るのはここ。最低限この6ケースを確認する。

| # | 入力 | 期待する連続日数 | 期待する挫折回数 |
|---|---|---|---|
| 1 | 初日に ○ | 1 | 0 |
| 2 | ○ ○ ○ | 3 | 0 |
| 3 | ○ ○ × ○ | 1 | 1 |
| 4 | ○ ○ （1日未記録）○ | 1 | 1 |
| 5 | ○ △ ○ | 3 | 0 |
| 6 | 3日間何もせず今日開いた | 0 | 変わらず（次に ○ を付けた瞬間に +1） |

## 追加提案（未決定）

以下は仕様として決めていない。必要か判断すること。

- **グリッド表示（GitHub の草）** — 元々の動機だった「スクロールが長くなる」問題への回答はこれ。UI フェーズで検討する。
